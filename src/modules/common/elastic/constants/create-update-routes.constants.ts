export enum ProfileRoutes {
  create = '/professional-profiles',
  update = '/professional-profiles/:id'
}

export enum VacancyRoutes {
  create = '/vacancy',
  update = '/vacancy/:id',
  status = '/vacancy/status',
}

export enum CustomerRoutes {
  create = '/customers',
  update = '/customers/:id'
}

export enum FunnelsRoutes {
  create = '/funnel',
  update = '/funnel/:id'
}

export enum TemplateRoutes {
  create = '/templates',
  update = '/templates/:id',
  status = '/templates/status/:id'
}